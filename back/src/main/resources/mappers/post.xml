<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cowlevel.gloria.dugout.mapper.post.PostMapper">
    <select id="selectAllPosts" resultType="Post">
        SELECT  *
        FROM  post
        ORDER BY id
    </select>

    <select id="selectPostById" resultType="Post">
        SELECT *
        FROM post
        WHERE id   = #{id}
    </select>

    <insert id="insertPost" parameterType="Post">
        INSERT INTO post(
            title,
            content,
            user_id,
            created_at,
            updated_at,
            hits,
            comment_count,
            good_count,
            board_id,
            thumbnail
        ) VALUES (
            #{title},
            #{content},
            #{userId},
            #{createdAt},
            #{updatedAt},
            0
            0
            0
            #{boardId},
            #{thumbnail}
                 )
    </insert>

    <update id="updatePost" parameterType="Post">
        UPDATE  post
        SET  title          = #{title},
             content        = #{content},
             updated_at     = #{updatedAt},
             state_comment  = #{stateComment}
             thumbnail      = #{thumbnail}
        WHERE  id           = #{id}
    </update>

    <delete id="deletePostById" parameterType="Long">
        DELETE
        FROM post
        WHERE id = #{id}
    </delete>
</mapper>